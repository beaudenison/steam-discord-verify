services:
  steam-discord-verify:
    build: .
    container_name: steam-discord-verify
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
